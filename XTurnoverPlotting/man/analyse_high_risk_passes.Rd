% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/high_risk_passes.R
\name{analyse_high_risk_passes}
\alias{analyse_high_risk_passes}
\title{Analyse High-Risk Passes for a Specific Team}
\usage{
analyse_high_risk_passes(
  data,
  data_provider = "statsbomb",
  team,
  risk_column = "risk_category",
  cluster_count = 6
)
}
\arguments{
\item{data}{A data.frame containing pass-level data. Must include columns:
\code{team.name}, \code{x}, \code{y}, \code{x_end}, \code{y_end}, and
the \code{risk_column}.}

\item{data_provider}{Character string indicating pitch dimensions provider
(e.g., "statsbomb", "opta", "wyscout"). Default is "statsbomb".}

\item{team}{Character string naming the team to analyse.}

\item{risk_column}{Character string specifying the column identifying high-risk passes. Default is "risk_category".}

\item{cluster_count}{Integer indicating number of clusters for k-means. Default is 6.}
}
\value{
A list with elements:
\item{plot}{ggplot2 object of clustered high-risk passes}
\item{cluster_data}{Data frame of passes with cluster assignment}
\item{kmeans_result}{k-means clustering model object}
}
\description{
This function filters passes labeled as high-risk for a specified team,
performs k-means clustering on start and end locations, and returns a
ggplot visualisation of clustered pass locations.
}
\examples{
\dontrun{
result <- analyse_high_risk_passes(data, team = "Arsenal", cluster_count = 6)
print(result$plot)
head(result$cluster_data)
}

}
